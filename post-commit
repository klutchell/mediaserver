#!/bin/sh

# restore environment variables to .env files

env_files="plex.env common.env"

for file in ${env_files}
do
	mv "${file}.bak" "${file}"
done

echo "restored environment variables to ${env_files}"

git update-index --assume-unchanged ${env_files}
