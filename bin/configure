#!/usr/bin/env bash

# set verbose
set -v

# add user
usermod -aG docker "kyle"

# copy config file to change storage location
cp "$(dirname "$0")"/../graph.conf /etc/systemd/system/docker.service.d/graph.conf

# reload systemctl
systemctl daemon-reload

# restart docker service
systemctl restart docker
