plex.__CLOUDFLARE_DOMAIN__ {
    gzip
    # uncomment this to enable basic auth (blocks api token access)
    # basicauth / user1 password1
    proxy / plex:32400 {
        transparent
    }
    tls {
        dns cloudflare
    }
}
tautulli.__CLOUDFLARE_DOMAIN__ {
    gzip
    # uncomment this to enable basic auth (blocks api token access)
    # basicauth / user1 password1
    proxy / tautulli:8181 {
        header_upstream X-Forwarded-Host {host}
        transparent
    }
    tls {
        dns cloudflare
    }
}
nzbget.__CLOUDFLARE_DOMAIN__ {
    gzip
    # uncomment this to enable basic auth (blocks api token access)
    # basicauth / user1 password1
    proxy / nzbget:6789 {
        transparent
    }
    tls {
        dns cloudflare
    }
}
sonarr.__CLOUDFLARE_DOMAIN__ {
    gzip
    # uncomment this to enable basic auth (blocks api token access)
    # basicauth / user1 password1
    proxy / sonarr:8989 {
        transparent
    }
    tls {
        dns cloudflare
    }
}
radarr.__CLOUDFLARE_DOMAIN__ {
    gzip
    # uncomment this to enable basic auth (blocks api token access)
    # basicauth / user1 password1
    proxy / radarr:7878 {
        transparent
    }
    tls {
        dns cloudflare
    }
}
hydra.__CLOUDFLARE_DOMAIN__ {
    gzip
    # uncomment this to enable basic auth (blocks api token access)
    # basicauth / user1 password1
    proxy / hydra:5076 {
        transparent
    }
    tls {
        dns cloudflare
    }
}
transmission.__CLOUDFLARE_DOMAIN__ {
    gzip
    # uncomment this to enable basic auth (blocks api token access)
    # basicauth / user1 password1
    proxy / transmission:9091 {
        transparent
    }
    tls {
        dns cloudflare
    }
}
